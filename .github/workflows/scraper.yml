name: Daily Sports Bet Scanner

on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  workflow_dispatch:

jobs:
  scan-for-opportunities:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout code
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    # Step 2: Setup Python
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    # Step 3: Install system dependencies (FIXED for Ubuntu 24.04)
    - name: 🔧 Install system dependencies
      run: |
        sudo apt-get update
        
        # Install essential build tools
        sudo apt-get install -y \
          python3-dev \
          build-essential \
          wget \
          unzip \
          curl \
          xvfb
        
        # Install Chrome dependencies (updated for Ubuntu 24.04)
        sudo apt-get install -y \
          libnss3 \
          libatk-bridge2.0-0t64 \
          libdrm2 \
          libxkbcommon0 \
          libxcomposite1 \
          libxdamage1 \
          libxrandr2t64 \
          libgbm1 \
          libpango-1.0-0 \
          libcairo2 \
          libasound2t64 \
          libxshmfence1 \
          libgl1-mesa-glx \
          libglib2.0-0t64
        
        echo "✅ System dependencies installed"

    # Step 4: Setup Chrome with CI optimizations
    - name: 🌐 Setup Chrome for CI
      run: |
        echo "=== Setting up Chrome for CI ==="
        
        # Install Chrome from Google's official repository
        wget -q -O /tmp/chrome.deb https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
        sudo apt-get install -y /tmp/chrome.deb
        
        # Verify Chrome installation
        echo "Chrome version:"
        google-chrome --version || echo "Chrome not installed"
        
        # Setup Xvfb display for headless Chrome
        echo "Starting Xvfb display server..."
        Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
        export DISPLAY=:99
        
        echo "✅ Chrome setup complete"

    # Step 5: Install Python packages
    - name: 📦 Install Python packages
      run: |
        pip install --upgrade pip
        pip install selenium==4.15.0
        pip install python-dotenv==1.0.0
        pip install requests==2.31.0
        pip install supabase==1.1.1
        pip install fuzzywuzzy==0.18.0
        pip install python-Levenshtein==0.25.1
        pip install pandas==1.5.0
        pip install numpy==1.24.0
        pip install beautifulsoup4==4.11.0
        pip install lxml==4.9.0
        pip install rich==12.6.0
        pip install webdriver-manager==4.0.1

    # Step 6: Install R packages (simplified)
    - name: 📦 Install R packages
      run: |
        echo "=== Installing R packages ==="
        
        # Install R
        sudo apt-get install -y r-base r-base-dev
        
        # Install minimal R packages for bet resolution
        Rscript -e "
        # Create user library
        dir.create('~/R/library', recursive = TRUE, showWarnings = FALSE)
        .libPaths(c('~/R/library', .libPaths()))
        
        # Install essential packages
        cat('Installing jsonlite...\\n')
        install.packages('jsonlite', lib = '~/R/library', repos = 'https://cloud.r-project.org')
        
        cat('Installing httr...\\n')
        install.packages('httr', lib = '~/R/library', repos = 'https://cloud.r-project.org')
        
        cat('Installing dplyr...\\n')
        install.packages('dplyr', lib = '~/R/library', repos = 'https://cloud.r-project.org')
        
        cat('\\nR packages installed successfully\\n')
        "

    # Step 7: Create CI-optimized scraper patch
    - name: 🔧 Create CI Scraper Patch
      run: |
        echo "=== Creating CI-optimized scraper patch ==="
        
        cat > ci_scraper_patch.py << 'EOF'
        """
        CI-optimized patch for UltraStableScanner
        """
        import os
        import sys
        
        def patch_ultra_stable_scanner():
            """Patch the UltraStableScanner for CI"""
            print("=== APPLYING CI OPTIMIZATIONS TO SCRAPER ===")
            
            try:
                # Import the scanner module
                import importlib.util
                import importlib
                
                # First, try to reload the module if already loaded
                if 'scraper.ultra_stable_scanner' in sys.modules:
                    importlib.reload(sys.modules['scraper.ultra_stable_scanner'])
                
                # Import fresh
                from scraper.ultra_stable_scanner import UltraStableScanner
                
                # Store original method
                original_init = UltraStableScanner.initialize_webdriver
                
                def ci_initialize_webdriver(self):
                    """CI-optimized WebDriver initialization"""
                    try:
                        # Close existing driver
                        if self.driver:
                            try:
                                self.driver.quit()
                            except:
                                pass
                            self.driver = None
                        
                        print("🔄 Initializing Chrome with CI optimizations...")
                        
                        from selenium import webdriver
                        from selenium.webdriver.chrome.options import Options
                        
                        chrome_options = Options()
                        
                        # ESSENTIAL for CI
                        chrome_options.add_argument("--no-sandbox")
                        chrome_options.add_argument("--disable-dev-shm-usage")
                        chrome_options.add_argument("--disable-gpu")
                        chrome_options.add_argument("--window-size=1920,1080")
                        
                        # Use new headless mode
                        chrome_options.add_argument("--headless=new")
                        
                        # Anti-detection
                        chrome_options.add_argument("--disable-blink-features=AutomationControlled")
                        chrome_options.add_experimental_option("excludeSwitches", ["enable-automation"])
                        chrome_options.add_experimental_option('useAutomationExtension', False)
                        
                        # Performance
                        chrome_options.add_argument("--disable-extensions")
                        chrome_options.add_argument("--disable-background-timer-throttling")
                        
                        # User agent
                        chrome_options.add_argument("--user-agent=Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36")
                        
                        # Additional CI optimizations
                        chrome_options.add_argument("--disable-dev-shm-usage")
                        chrome_options.add_argument("--disable-software-rasterizer")
                        
                        # Create driver
                        try:
                            from webdriver_manager.chrome import ChromeDriverManager
                            from selenium.webdriver.chrome.service import Service as ChromeService
                            
                            print("   Using webdriver-manager for ChromeDriver...")
                            service = ChromeService(ChromeDriverManager().install())
                            driver = webdriver.Chrome(service=service, options=chrome_options)
                        except Exception as e:
                            print(f"   webdriver-manager failed: {e}, using fallback...")
                            driver = webdriver.Chrome(options=chrome_options)
                        
                        # Set CI-optimized timeouts
                        driver.set_page_load_timeout(60)  # 60 seconds
                        driver.implicitly_wait(15)
                        
                        # Hide automation
                        try:
                            driver.execute_cdp_cmd('Page.addScriptToEvaluateOnNewDocument', {
                                'source': '''
                                    Object.defineProperty(navigator, 'webdriver', {
                                        get: () => undefined
                                    });
                                '''
                            })
                        except:
                            pass
                        
                        print("✅ CI-optimized ChromeDriver ready")
                        return driver
                        
                    except Exception as e:
                        print(f"❌ CI WebDriver initialization failed: {e}")
                        import traceback
                        traceback.print_exc()
                        return None
                
                # Apply patch
                UltraStableScanner.initialize_webdriver = ci_initialize_webdriver
                print("✅ UltraStableScanner patched for CI environment")
                
            except Exception as e:
                print(f"⚠️ Could not patch scanner: {e}")
                import traceback
                traceback.print_exc()
        
        # Run the patch
        patch_ultra_stable_scanner()
        EOF
        
        echo "✅ CI scraper patch created"

    # Step 8: Run the Enhanced Paper Trading System
    - name: 🎯 Run Enhanced Paper Trading System
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        DISPLAY: ':99'
      run: |
        echo "🚀 Running Enhanced Paper Trading System"
        echo "=========================================="
        echo "Current time: $(date)"
        
        # Simple, reliable runner
        cat > run_simple.py << 'EOF'
        import os
        import sys
        import time
        import traceback
        from datetime import datetime
        
        # Setup environment
        os.environ['DISPLAY'] = ':99'
        sys.path.insert(0, os.getcwd())
        
        print("=" * 60)
        print("ENHANCED PAPER TRADING - SIMPLE CI RUNNER")
        print(f"Started: {datetime.now()}")
        print("=" * 60)
        
        def main():
            try:
                # 1. Apply CI patches
                print("\\n1. Applying CI optimizations...")
                try:
                    import ci_scraper_patch
                    print("   ✅ CI scraper patch applied")
                except Exception as e:
                    print(f"   ⚠️ CI patch failed: {e}")
                
                # 2. Import and initialize system
                print("\\n2. Initializing system...")
                from enhanced_paper_trading import EnhancedPaperTradingSystem
                system = EnhancedPaperTradingSystem()
                print(f"   ✅ System initialized")
                print(f"   Bankroll: €{system.bankroll:.2f}")
                print(f"   Pending bets: {len(system.db.get_pending_bets())}")
                
                # 3. Run automated scanning (with timeout protection)
                print("\\n3. Running automated scanning...")
                try:
                    # Test if website is accessible first
                    import requests
                    print("   Testing website connection...")
                    try:
                        resp = requests.get('https://crazyninjaodds.com', timeout=10)
                        if resp.status_code == 200:
                            print("   ✅ Website is accessible")
                        else:
                            print(f"   ⚠️ Website status: {resp.status_code}")
                    except Exception as e:
                        print(f"   ⚠️ Website check failed: {e}")
                    
                    # Run scanning
                    print("   Starting scan...")
                    system.automated_scanning(max_scans=1)
                    print("   ✅ Scanning completed")
                except Exception as e:
                    print(f"   ⚠️ Scanning failed (may be expected): {e}")
                
                # 4. Resolve pending bets (MOST IMPORTANT)
                print("\\n4. Resolving pending bets...")
                try:
                    system._resolve_pending()
                    print("   ✅ Bet resolution completed")
                except Exception as e:
                    print(f"   ❌ Bet resolution failed: {e}")
                    traceback.print_exc()
                
                # 5. Show performance summary
                print("\\n5. Performance summary:")
                try:
                    stats = system.db.get_performance_stats()
                    pending = len(system.db.get_pending_bets())
                    
                    print(f"   Bankroll: €{system.bankroll:.2f}")
                    print(f"   Total Profit: €{stats.get('total_profit', 0):.2f}")
                    print(f"   Total Bets: {stats.get('total_bets', 0)}")
                    print(f"   Won: {stats.get('won_bets', 0)} | Lost: {stats.get('lost_bets', 0)}")
                    print(f"   Pending: {pending}")
                    
                    if stats.get('total_bets', 0) > 0:
                        win_rate = (stats.get('won_bets', 0) / stats.get('total_bets', 0)) * 100
                        print(f"   Win Rate: {win_rate:.1f}%")
                except Exception as e:
                    print(f"   ⚠️ Performance summary failed: {e}")
                
                print("\\n" + "=" * 60)
                print(f"COMPLETED: {datetime.now()}")
                print("✅ Workflow completed successfully!")
                print("=" * 60)
                
                return True
                
            except Exception as e:
                print(f"\\n❌ CRITICAL ERROR: {e}")
                traceback.print_exc()
                return False
        
        if __name__ == "__main__":
            success = main()
            sys.exit(0 if success else 1)
        EOF
        
        # Run the simple runner
        echo "\\n=== EXECUTING SIMPLE RUNNER ==="
        python run_simple.py
        
        EXIT_CODE=$?
        
        if [ $EXIT_CODE -eq 0 ]; then
            echo "\\n✅ GitHub Actions workflow completed SUCCESSFULLY!"
        else
            echo "\\n⚠️ Workflow had errors, trying fallback..."
            
            # Ultra-simple fallback - just bet resolution
            echo "\\n=== ULTRA SIMPLE FALLBACK ==="
            python3 -c "
            import os
            import sys
            sys.path.insert(0, os.getcwd())
            
            print('Running minimal bet resolution...')
            
            try:
                from enhanced_paper_trading import EnhancedPaperTradingSystem
                system = EnhancedPaperTradingSystem()
                print(f'✅ System: €{system.bankroll:.2f}')
                
                # Just resolve bets
                if hasattr(system, '_resolve_pending'):
                    system._resolve_pending()
                    print('✅ Bets resolved')
                else:
                    print('⚠️ Could not resolve bets')
                    
            except Exception as e:
                print(f'❌ Fallback failed: {e}')
            "
        fi
        
        echo "\\n🏁 Workflow execution finished!"
